_schema-version: '3.1'
ID: freestylesapui5app
version: 0.0.1

modules:
  - name: freestylesapui5app
    type: staticfile
    path: webapp
    parameters:
      memory: 128M
      disk-quota: 256M
      routes:
        - route: freestylesapui5app.${default-domain}

resources: []
